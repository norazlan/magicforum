<%= form_for(@comment, url: topic_post_comments_path, html: { id: "topic-create-form" }) do |f| %>
  <h1 class="text-center">
    Create Comment for "<%= @post.title %>"
  </h1>

  <div class="form-group">
    <h4> <%= f.label :body_field, "Your Comment" %> </h4>
    <%= f.text_field :body, required: true, id: "post_body_field", class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.file_field :image, id: "post_image_field", class: 'form-control image-uploader' %>
  </div>

  <div class="form-group text-center">
    <%= f.submit "Create Comment", class: "btn btn-success btn-lg" %>
  </div>
<% end %>
